@if(isset($title))
    <div class="banner1">
        <div class="wthree_banner1_info">
                <h3 data-aos="fade-up" data-aos-duration="1000"><span>{{substr($title,0,1)}}</span>{{substr($title,1)}}</h3>
        </div>
    </div>
@endif